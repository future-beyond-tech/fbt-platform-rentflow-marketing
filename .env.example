# Optional: Base URL for Open Graph and metadata (default: https://rentflow.in)
# NEXT_PUBLIC_SITE_URL=https://rentflow.in

# ---- Lean Validation Backend ----

# Database (Neon) - required for waitlist, investors, ROI analytics
# DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# Email (Resend) - for waitlist welcome & investor lead notifications
# RESEND_API_KEY="re_xxxxxxxx"

# Rate limiting (Upstash Redis) - optional; without it, rate limits are skipped
# UPSTASH_REDIS_REST_URL="https://xxxx.upstash.io"
# UPSTASH_REDIS_REST_TOKEN="xxxx"

# Admin export - secret key for GET /api/admin/export?key=...&table=waitlist|investors|roi
# ADMIN_SECRET_KEY="your-secret-key-here"

# Enable Lean Backend: set to true when using waitlist/investors/ROI APIs.
# When true, disables static export so API routes run (deploy to Vercel).
# ENABLE_LEAN_BACKEND=true
