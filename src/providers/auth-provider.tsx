"use client";

import React from "react";

/**
 * Placeholder for auth provider (e.g. session, auth context).
 * Wrap children with AuthProvider when needed.
 */
export function AuthProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
