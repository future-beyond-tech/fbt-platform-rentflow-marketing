"use client";

import React from "react";

/**
 * Placeholder for React Query / TanStack Query provider.
 * Install @tanstack/react-query and wrap children with QueryClientProvider.
 */
export function QueryProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
