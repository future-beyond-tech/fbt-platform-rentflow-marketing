"use client";

import React from "react";

/**
 * Placeholder for theme provider (e.g. next-themes).
 * Wrap children with ThemeProvider when needed.
 */
export function ThemeProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
